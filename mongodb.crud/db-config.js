module.exports = {
  /* The connection string with a host, a port and the db name. */
  url: 'mongodb://localhost:27017/crud',
  /* the name of document collection that should be used by DAO. */
  collection: 'users'
};
